/*
 * Historical experimental shell path placeholder.
 *
 * The legacy single-loop shell implementation has been superseded by the
 * current RTOS task graph, service bus, display runtime, power runtime, and
 * ESP sync service. The old implementation was kept only as commented history,
 * which made the codebase noisy during maintenance.
 *
 * This file intentionally contains no executable code. It remains in the tree
 * as a temporary placeholder so the next cleanup phase can archive or remove it
 * in a controlled way together with the APP1-only project entry.
 */
